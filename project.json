{
  "name": "reservoir_computing",
  "root": "packages/reservoir_computing", 
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["python -m pip install build", "python -m build"],
        "cwd": "packages/reservoir_computing"
      }
    },
    "test": {
      "executor": "nx:run-commands", 
      "options": {
        "commands": ["python -m pytest . -v"],
        "cwd": "packages/reservoir_computing"
      }
    }
  }
}
